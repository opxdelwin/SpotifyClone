import 'package:flutter/material.dart';

final Color darkBlue = Color.fromARGB(255, 18, 32, 47);

void main() {
  runApp(MaterialApp(
  home: Home(),
  ));
}

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(
    backgroundColor: Colors.grey[700],
    title: Text('Good Evening'),
    ), 
    body: 
      Container(
      padding: EdgeInsets.all(5),
      color: Colors.grey[800],
      child: Column(
      children: <Widget>[
        Column(
        children: <Widget> [
          Row(
          children: <Widget> [
            Expanded(child: Container(
              decoration: BoxDecoration(
              color: Colors.grey,
              borderRadius: BorderRadius.all(Radius.circular(3))),
              padding: EdgeInsets.all(5.0),
              child: Center(child: Text('Recently Played', style: TextStyle(
                                             
                                             color: Colors.white),))
            ),),
            SizedBox(width: 5),
            Expanded(child: Container(
              decoration: BoxDecoration(
              color: Colors.grey,
              borderRadius: BorderRadius.all(Radius.circular(3))),
              padding: EdgeInsets.all(5.0),
              child: Center(child: Text('Liked songs', style: TextStyle(
                                             color: Colors.white),))))
          ]),
        ]
        ),
        SizedBox(height: 10),
        
        Row(
        children: <Widget> [
          Container(
          width: 132,
          height: 155,
          decoration: BoxDecoration(
            color: Colors.green,
            borderRadius: BorderRadius.all(Radius.circular(10)))
          ), SizedBox(width: 5.0),
          Container(
          width: 132,
          height: 155,
          decoration: BoxDecoration(
            color: Colors.green,
            borderRadius: BorderRadius.all(Radius.circular(10))
          ))
        ]
        ),
      ],
      ),
      ), 
    );
  }
}
